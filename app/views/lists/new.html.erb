<% simple_form_for(@list) do |f| %>

  <div class="form-inputs">
    <% f.input :name %>
  </div>

  <div class="form-iactions">
    <% f.button :submit %>
  </div>
  <% end %>
  <% link_to "Back to list", lists_path %>
